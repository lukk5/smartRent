import { UserProp } from "../models/userModel";

const Documents: React.FC<UserProp> = (props) => {
    return ( <main style={{ padding: "1rem" }}>
    <p>Documents!</p>
  </main>);
};

export default Documents;